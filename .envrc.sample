export KAMAL_REGISTRY_PASSWORD=  # token do Docker Hub (hub.docker.com → Account Settings → Personal access tokens)
export RAILS_MASTER_KEY=         # conteúdo de config/master.key
export ALLOWED_ORIGINS=          # URL do frontend (ex: https://app.exemplo.com)
